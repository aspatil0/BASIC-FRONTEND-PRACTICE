import React from 'react';
import Cat from './components/Cat';
import Counter from './components/Counter';

function App() {
  return (
   // <Cat  name='adi'/>  it is the props
    // when we take many data in cat we can use props to dynamicaly chnage elements
  );
}

export default App;
